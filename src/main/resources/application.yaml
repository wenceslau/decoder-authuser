server:
  port: 8087

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ead-authuser
    username: sysm
    password: '@S1smp'
  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.PostgreSQLDialect
      jdbc:
        lob.non-contextual-creation: true #Desabilita busca de meta info do hibernate na inicializa�ao
    properties:
      hibernate:
        show_sql: true